import { Component } from "solid-js";

const Header: Component = () => {
  return (
    <header>
      <nav class="navbar bg-base-100">
        <div class="flex-1">
          <h1 class="text-2xl">Estatística</h1>
        </div>
      </nav>
    </header>
  );
};

export default Header;
